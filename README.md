# Checksheet Server (Backend)

ระบบ Backend สำหรับจัดการ E-Checksheet โดยใช้ Node.js และ Express ร่วมกับฐานข้อมูล PostgreSQL สนับสนุนการทำงานร่วมกับระบบ Admin Panel และหน้าฟอร์มตรวจสอบเครื่องจักร

## 🛠 Tech Stack

- **Runtime**: Node.js
- **Framework**: Express.js
- **Database**: PostgreSQL (pg)
- **Authentication**: JSON Web Token (JWT) & Cookies
- **Security**: Bcrypt (Password Hashing)
- **Process Management**: Nodemon

## 🚀 คุณสมบัติหลัก (Key Features)

- **User Authentication**: ระบบ Login/Logout พร้อมการจัดการ Session ผ่าน Cookie-based JWT
- **User Management**: จัดการข้อมูลพนักงาน (เพิ่ม, แก้ไข, ลบ) และกำหนดหน้าที่ (Roles)
- **Activity Logging**: บันทึกประวัติการใช้งาน (Login, การแก้ไขข้อมูล, ลบข้อมูล) ลงฐานข้อมูล
- **Form API**: จัดการข้อมูลฟอร์ม และบันทึกข้อมูล Checksheet ลง PostgreSQL
- **Admin Dashboard**: ให้บริหารหน้า Admin Panel แบบ SPA (Built via React)
- **Nginx Compatible**: รองรับการทำงานผ่าน Proxy (Nginx) พร้อมระบบ Trust Proxy

---

## ⚙️ การติดตั้ง (Setup)

### 1. ติดตั้ง Dependencies
```bash
npm install
```

### 2. ตั้งค่าฐานข้อมูล (PostgreSQL)
สร้างฐานข้อมูลใน PostgreSQL และรัน SQL ตามโครงสร้างตารางที่ใช้ (Users, Activity_Logs, และตารางข้อมูลฟอร์ม)

### 3. ตั้งค่าสภาพแวดล้อม (Environment Variables)
สร้างไฟล์ `.env` ที่ root directory และกำหนดค่าดังนี้:
```env
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=checksheet_db
DB_PORT=5432
SERVER_PORT=3000

# Security (Optional)
JWT_SECRET=your_jwt_secret
```

---

## 🏃 การรันระบบ (Running)

**โหมดพัฒนา (Development):**
```bash
npm start
```

---

## 🌐 API Endpoints (Overview)

| Path | Method | Description |
| :--- | :--- | :--- |
| `/auth/login` | POST | ล็อกอินเข้าระบบ |
| `/auth/logout` | POST | ล็อกเอาท์ |
| `/auth/me` | GET | ตรวจสอบข้อมูลผู้ใช้ปัจจุบัน |
| `/users` | GET/POST | จัดการข้อมูลผู้ใช้ (Admin Only) |
| `/logs` | GET | ดูประวัติกิจกรรมย้อนหลัง (Admin Only) |
| `/api/save-form` | POST | บันทึกข้อมูล Checksheet |
| `/api/load-form/:id` | GET | ดึงข้อมูล Checksheet ตาม ID |

---

## 🛡 การตั้งค่า Nginx (Deployment Tips)

เพื่อให้ระบบ Cookie และ Authentication ทำงานได้ถูกต้องผ่าน Nginx ควรตั้งค่า `proxy_set_header` ดังนี้:

```nginx
location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

---

## 📂 โครงสร้างโฟลเดอร์

- `/config`: การตั้งค่าการเชื่อมต่อฐานข้อมูล
- `/routes`: จัดแบ่งกลุ่ม API (Auth, Users, Forms, Logs)
- `/middleware`: ระบบตรวจสอบสิทธิ์ (Auth Middleware)
- `/utils`: เครื่องมือช่วยเหลือ (Logger)
- `/checksheet_admin`: โปรแกรมหน้าจัดการสำหรับ Admin
- `/checksheet_form`: ฟอร์มอิเล็กทรอนิกส์สำหรับ Workers
